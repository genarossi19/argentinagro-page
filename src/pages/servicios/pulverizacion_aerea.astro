---
import { Button } from "@/components/ui/button";
import Layout from "@/layouts/Layout.astro";
import {
  MessageCircle,
  Plane,
  Target,
  Cpu,
  Droplets,
  Gauge,
  CheckCircle,
} from "lucide-react";
import "@/styles/global.css";
import { CarouselDemo } from "@/components/Carousel/CarouselDemo";
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Separator } from "@/components/ui/separator";
import { PulverizacionFAQ } from "@/components/PulverizacionFAQ";

const images = [
  "/pulverizacion_aerea2.jpg?height=300&width=300",
  "/pulverizacion_aerea3.jpg?height=300&width=300",
  "/pulverizacion_aerea4.jpg?height=300&width=300",
  "/pulverizacion_aerea5.jpg?height=300&width=300",
];

const title = "Pulverización aérea";

// Datos para las características clave
const caracteristicas = [
  {
    titulo: "Tecnología de precisión",
    descripcion:
      "Aviones equipados con computadoras agrícolas y banderillero satelital para una distribución uniforme.",
    icono: "Cpu",
  },
  {
    titulo: "Aplicaciones versátiles",
    descripcion:
      "Aplicación precisa de insecticidas, fungicidas y herbicidas según las necesidades de cada cultivo.",
    icono: "Droplets",
  },
  {
    titulo: "Logística completa",
    descripcion:
      "Vehículo de apoyo con combustibles, motobombas, productos y agua para asegurar independencia operativa.",
    icono: "Gauge",
  },
];
---

<Layout title=`Servicios | ${title}`>
  <div class="h-[120px] header-blur w-full z-10 fixed top-0"></div>
  <section class="w-[95%] mx-auto mt-28 mb-28">
    <h1
      class="text-[40px] font-semibold text-center text-logo-blue underline"
      transition:name=`${title}-title`
    >
      {title}
    </h1>
    <div class="mt-10 w-full h-[300px]">
      <img
        transition:name="img-pulverizacion_aerea"
        class="object-center object-cover h-full w-full rounded-md"
        src="/pulverizacion_aerea.jpg"
        alt={title}
      />
    </div>
    <div
      transition:animate="fade"
      class="mt-10 flex flex-col md:flex-row gap-8 items-start"
    >
      <div class="w-full md:w-2/3 px-4">
        <Card
          className="border-0 shadow-sm bg-white/80 backdrop-blur-sm h-full"
        >
          <CardHeader className="pb-2">
            <div class="flex flex-col gap-4">
              <Badge
                variant="outline"
                className="w-fit px-4 py-1 text-sm tracking-wider text-logo-blue border-logo-blue/20 bg-logo-blue/5 rounded-full"
              >
                Protección de cultivos
              </Badge>
              <div class="space-y-2">
                <CardTitle
                  className="text-4xl font-bold tracking-wider text-gray-900"
                >
                  Maximiza tus rendimientos con una pulverización aérea precisa
                  y oportuna
                </CardTitle>
                <CardDescription
                  className="text-xl tracking-wider text-gray-500"
                >
                  Tecnología de vanguardia para la protección eficiente de sus
                  cultivos
                </CardDescription>
              </div>
            </div>
          </CardHeader>

          <CardContent className="pt-8">
            <div class="space-y-8">
              <!-- Introducción con imagen de fondo -->
              <div
                class="relative overflow-hidden rounded-xl p-6 bg-gradient-to-r from-logo-blue/20 to-logo-blue/5"
              >
                <div class="relative z-10">
                  <div class="flex items-start gap-4 mb-4">
                    <div
                      class="flex h-12 w-12 shrink-0 items-center justify-center rounded-full bg-white/80 text-logo-blue"
                    >
                      <Plane className="h-6 w-6" />
                    </div>
                    <h3
                      class="text-xl font-semibold tracking-wider text-gray-900"
                    >
                      Solución integral para sus cultivos
                    </h3>
                  </div>
                  <p class="text-gray-700 tracking-wider leading-relaxed">
                    Para aplicaciones de insecticidas, fungicidas y herbicidas,
                    disponemos de dos aviones equipados con
                    <span class="font-semibold"
                      >computadoras agrícolas y banderillero satelital</span
                    >, asegurando buena distribución del producto sobre el
                    cultivo en tratamiento, eliminando subdosis o sobredosis que
                    afectan la uniformidad de distribución.
                  </p>
                </div>
              </div>

              <!-- Características clave -->
              <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                {
                  caracteristicas.map((caracteristica) => (
                    <div class="p-5 rounded-lg border border-logo-blue/10 hover:bg-logo-blue/5 transition-colors">
                      <div class="flex flex-col gap-3">
                        <div class="flex h-10 w-10 items-center justify-center rounded-full bg-logo-blue/10 text-logo-blue">
                          {caracteristica.icono === "Cpu" && (
                            <Cpu className="h-5 w-5" />
                          )}
                          {caracteristica.icono === "Droplets" && (
                            <Droplets className="h-5 w-5" />
                          )}
                          {caracteristica.icono === "Gauge" && (
                            <Gauge className="h-5 w-5" />
                          )}
                        </div>
                        <h3 class="font-semibold tracking-wider text-gray-900">
                          {caracteristica.titulo}
                        </h3>
                        <p class="text-gray-600 tracking-wider text-sm">
                          {caracteristica.descripcion}
                        </p>
                      </div>
                    </div>
                  ))
                }
              </div>

              <!-- Destacado de logística -->
              <div
                class="bg-logo-blue/5 p-6 rounded-lg border border-logo-blue/20"
              >
                <div class="flex items-start gap-4">
                  <div
                    class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-logo-blue/10 text-logo-blue mt-1"
                  >
                    <CheckCircle className="h-5 w-5" />
                  </div>
                  <p class="text-gray-600 tracking-wider">
                    Para todas las alternativas de fertilización, siembra o
                    pulverización líquida, complementamos el servicio con un
                    <span class="font-semibold"> vehículo de apoyo</span> que transporta
                    combustibles, motobombas, productos y agua, asegurando independencia
                    operativa y comodidad en el servicio.
                  </p>
                </div>
              </div>

              <Separator className="my-6" />

              <!-- Preguntas frecuentes como componente React -->
              <PulverizacionFAQ client:load />
            </div>
          </CardContent>

          <CardFooter className="pt-6">
            <a
              target="_blank"
              href="https://api.whatsapp.com/send?phone=542392480555&text=%C2%A1Hola!%2C%20te%20hago%20una%20consulta."
              class="w-full sm:w-auto"
            >
              <Button
                className="w-full sm:w-auto bg-black hover:bg-black/90 text-white gap-3 group px-6 py-6 text-base tracking-wider"
                size="lg"
              >
                <span>Saber más</span>
                <MessageCircle
                  size={20}
                  className="transition-transform group-hover:translate-x-1"
                />
              </Button>
            </a>
          </CardFooter>
        </Card>
      </div>
      <div class="w-full md:w-1/3 sticky top-32">
        <div class="rounded-lg overflow-hidden shadow-sm">
          <CarouselDemo client:load images={images} />
        </div>
      </div>
    </div>
  </section>
</Layout>
